# GitHub SaaS measurement fixture

This repository is disposable and exists to generate controlled GitHub API,
Audit Log, webhook, Git, and GitHub Actions observations.

Resources use the `NEMO-LAB` marker so observations can be correlated without
placing credentials or production data in the repository.
